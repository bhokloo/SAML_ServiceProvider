@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
        <ul class="nav navbar-nav navbar-right">
            <li>
                @Html.ActionLink("Hello " + User.Identity.GetUserName() + "!", "About", "Home", routeValues: null, htmlAttributes: new { title = "Manage" })
            </li>
        </ul>
    }
}